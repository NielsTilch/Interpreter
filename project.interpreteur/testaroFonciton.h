int execution (const char* line);

int execCompa(char* lineCommand, char* lineSortie);

int executionToStdin(char* line, char* stdin);

int execCompaStdin(char *line, char* bufferStdout, char* bufferStdin);